{
  "name": "modernia",
  "private": true,
  "version": "1.0.1",
  "author": "夏源",
  "description": "Modernia — 桌面工具箱",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build && electron-builder",
    "build:mac": "vue-tsc && vite build && electron-builder --mac",
    "build:win": "vue-tsc && vite build && electron-builder --win",
    "build:linux": "vue-tsc && vite build && electron-builder --linux",
    "release": "vue-tsc && vite build && electron-builder --publish always",
    "release:mac": "vue-tsc && vite build && electron-builder --mac --publish always",
    "release:win": "vue-tsc && vite build && electron-builder --win --publish always",
    "release:linux": "vue-tsc && vite build && electron-builder --linux --publish always",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vicons/ionicons5": "^0.13.0",
    "apk-info-parser": "^1.1.0",
    "electron-updater": "^6.8.9",
    "naive-ui": "^2.45.3",
    "spark-md5": "^3.0.2",
    "vue": "^3.4.21",
    "vue-json-pretty": "^2.6.0",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@types/semver": "^7.8.0",
    "@types/spark-md5": "^3.0.5",
    "@vitejs/plugin-vue": "^5.0.4",
    "electron": "^30.0.1",
    "electron-builder": "^24.13.3",
    "typescript": "^5.2.2",
    "vite": "^5.1.6",
    "vite-plugin-electron": "^0.28.6",
    "vite-plugin-electron-renderer": "^0.14.5",
    "vue-tsc": "^2.0.26"
  },
  "main": "dist-electron/main.js"
}
